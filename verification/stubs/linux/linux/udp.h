#ifndef _LINUX_UDP_H
#define _LINUX_UDP_H

/* Vigor: Trimmed down to essential includes only. */

#include <net/sock.h>

/* Vigor: Define just enough to compile. */

struct udp_sock {
};

#endif /* _LINUX_UDP_H */
